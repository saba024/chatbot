@model chatbot.Models.Dish

<div class="col-lg-4">
    <img class="img-thumbnail" src="@Model.Img" alt="@Model.Name" width="300" height="300" />
    <h2>@Model.Name</h2>
    <p>@Model.Description</p>
    <p>@Model.Price.ToString("c")</p>
</div>